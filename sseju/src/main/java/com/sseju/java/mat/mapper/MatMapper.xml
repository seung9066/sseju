<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sseju.java.mat.mapper.MatMapper">
	<select id="matBuyList" resultType="MatVO">
		SELECT * FROM mat_buy
	</select>
	<select id="selectMatbuyInfo" resultType="MatVO">
		SELECT * FROM mat_buy WHERE mat_ord_name = #{matOrdName}
	</select>
	<insert id="insertMatbuy" parameterType="MatVO">
		<selectKey keyProperty="matOrdName" resultType="String"
			order="BEFORE">
			SELECT 'MAT_ORD_'||LPAD(NVL(MAX(SUBSTR(mat_ord_name, -3)),0)+1,3,0) FROM
			may_buy
		</selectKey>
		INSERT INTO mat_buy
		VALUES (
			#{matOrdName}
			,#{matOrdQty}
			,#{nowCode}
			,#{matCode}
 			,sysdate
			,#{cpCode}
			,#{matOrdYn}
			,#{matPrice}
			,#{matOrdEmp}
		)
	</insert>
	<update id="updateMatbuy" parameterType="MatVO">
		UPDATE mat_buy
		<set>
			<if test="matOrdQty != null and !matOrdQty.equals('')">
			MAT_ORD_QTY = #{matOrdQty},			
			</if>
			<if test="nowCode != null and !nowCode.equals('')">
			NOW_CODE = #{nowCode},
			</if>
			<if test="matOrdDate != null and !matOrdDate.equals('')">
			MAT_ORD_DATE =#{matOrdDate},
			</if>
			<if test="cpCode != null and !cpCode.equals('')">
			CP_CODE = #{cpCode},
			</if>
			<if test="matOrdYn != null and !matOrdYn.equals('')">
			MAT_ORD_YN = #{matOrdYn},
			</if>
			<if test="matPrice != null and !matPrice.equals('')">
			MAT_PRICE = #{matPrice},
			</if>
			<if test="matOrdYn != null and !matOrdYn.equals('')">
			MAT_ORD_EMP = #{matOrdEmp}
			</if>
		</set>
		WHERE MAT_ORD_NAME = #{matOrdName}
	</update>
	<delete id="deleteMatbuy" parameterType="MatVO">
		DELETE FROM mat_buy WHERE MAT_ORD_NAME = #{matOrdName}
	</delete>
	
	<delete id="selectDelete" parameterType="list">
		DELETE FROM mat_buy WHERE MAT_ORD_NAME in(
		<foreach collection="list" item="item" separator=",">
			#{item}
		</foreach>
		)
	</delete>
</mapper>